import 'package:flutter/material.dart';

const primaryColor = Color(0xFFFF4C29);
const secondaryColor = Color(0xFF2C394B);
const darkColor = Color(0xFF334756);
const textColor = Color(0xFFEEEEEE);
const backColor = Color(0xFF082032);

const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 1); // we use it on our animation
const maxWidth = 1440.0; // max width of our web

// https://colorhunt.co/palette/0820322c394b334756ff4c29
